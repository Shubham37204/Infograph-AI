[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "B", "W", "I"]
ignore = []

[tool.pytest.ini_options]
addopts = "-v --asyncio-mode=auto"
testpaths = [
    "tests",
]
